# Data model decision pack

Prepared for [issue #2](https://github.com/itsamattbuild/INVESTMENT-PORTFOLIO/issues/2).
Nothing here decides anything — #2 is a grilling ticket and stays open. What this
document does is replace three of #2's seven questions ("argue it out") with
measured facts ("pick one of these two rows"), and attach recommendations to the
remaining four.

**Test conditions.** Python 3.14.4, SQLite 3.46.1 (via `sqlite3` module), Linux
container, 2026-08-26. The interrupted-write tests force `fsync` at every step
and kill writers with `SIGKILL`, so the results are disk-level truth, not
buffering luck. Scripts: `bench/` in this branch (deterministic seed 42).
One environment caveat: macOS specifics (e.g. `/usr/bin/sqlite3` shipping with
the OS) were reasoned from knowledge, not tested here.

The synthetic dataset: **500 transactions across 12 tickers**, generated by
`make_transactions(500)` — buys/sells/splits at realistic prices with fractional
shares to 4 dp, ~4% splits, ~21% sells, commissions on every trade.

---

## A. SQLite versus JSON — both built, both measured

Same logical data written through two tiny stores (`JsonStore`, `SqliteStore`
in `bench/common.py`). Both derive byte-identical positions (asserted in code).

### A1. Size and load time

| Metric (500 txns) | JSON (pretty-printed) | SQLite |
|---|---|---|
| File size | **133,783 B** | **53,248 B** |
| Load + derive all positions, median | **0.45 ms** | 0.91 ms |
| Load + derive, p90 | 0.52 ms | 0.98 ms |

At 5,000 transactions: JSON 1.34 MB / 5.5 ms; SQLite 377 kB / 9.9 ms.

Reading: **both are instant at any scale this app will ever reach.** SQLite is
2.5× smaller; JSON is 2× faster to load back. Neither number can decide this.

### A2. What `git diff` looks like after appending one transaction

| Store | `git diff --numstat` | Readable? |
|---|---|---|
| JSON, `indent=2`, rewritten wholesale | 13 insertions, 0 deletions | yes — one new object plus comma churn |
| JSON, one-object-per-line format | **+2 / −1** | yes — previous line gains a comma, new line added |
| SQLite, one row INSERTed | `- -` (**binary**) | no — "Binary files differ", unreviewable |

A line-oriented JSON layout (each transaction on one line, comma leading the
next) makes an append a two-line diff that reads like prose. Verified output:

```diff
 {"id": 500, "ticker": "BRK-B", ..., "date": "2022-02-07", ...}
+{"id": 501, "ticker": "AAPL", "kind": "buy", "shares": 3.1234,
+ "price": 309.9, ..., "note": "added while benchmarking"}
```

SQLite diffs are binary noise regardless of formatting choices.

### A3. Interrupted write — SIGKILL mid-write (the test that matters)

Methodology: writer children report progress over stdout; the parent lets them
get partway through and then sends `SIGKILL`. All writes are `fsync`-forced so
nothing hides in buffers. Five runs per configuration; every run identical.

| Configuration | What is left on disk | Recoverable? |
|---|---|---|
| **JSON rewritten in place** (naive `open(path,'w')`), killed ~45% through | 60,300 B of a ~134 kB document — a truncated half-document | **No.** Unparseable (`Expecting value`). The *whole ledger* is unreadable, not just the last transaction. Salvage by bracket-scanning recovered only ~225 of 501 objects, lossy, custom code required. |
| **JSON via temp file + `os.replace`** | Main file untouched — valid, complete old version. Plus a stray `.tmp` (itself truncated). | **Yes — always.** Old state survives every kill. Cleanup = delete `*.tmp` on next start. Cost measured: **+0.03 ms per write** (0.48 → 0.51 ms median for a full rewrite). |
| **SQLite, row-at-a-time commits** (WAL) | All committed rows present: 501 rows after kill. `PRAGMA integrity_check` = ok. No stray files. | Yes — automatic. Committed data survives; uncommitted work is lost, as designed. |
| **SQLite, all 500 rows in ONE transaction**, killed halfway | 0 rows visible. `integrity_check` = ok. | Yes — automatic all-or-nothing rollback. |

Reading: **the interrupted-write argument does not decide the question either,
because JSON has a two-line fix** (write temp → fsync → `os.replace`) whose cost
is unmeasurable. Without that pattern the naive JSON implementation destroys the
entire ledger on a crash mid-write — this is the one genuinely dangerous row in
the table. SQLite gives transactionality out of the box but its safety was never
in dispute.

### A4. Editing by hand

| Aspect | JSON | SQLite |
|---|---|---|
| Tool needed | any text editor | SQL client (on macOS: preinstalled `/usr/bin/sqlite3`; **absent in this test container**) |
| Syntax error while editing | caught instantly by the parser on next load (`Expecting ',' delimiter line 17` in test) | impossible to make in a text editor; SQL syntax errors rejected by the engine |
| Plausible-but-wrong value (price digit changed) | **accepted silently** — nothing flags it | also accepted if typed correctly; see below |
| Type enforcement | n/a (JSON types fixed) | default tables silently store `'not a number'` as TEXT in a REAL column (verified). `STRICT` tables reject it (verified: `IntegrityError`) |

Reading: hand-editability is JSON's genuine advantage, and the diff advantage is
real for a public repo where the user reviews history. But note what hand
editing means in practice: fixing a typo is easy in JSON, *making* a silent
wrong-but-valid edit is equally easy. Any store needs a load-time sanity check
(shares > 0 on sells, monotone dates) regardless of format.

### A5. Where this leaves question 1

| Property | JSON single-file + atomic rename | SQLite |
|---|---|---|
| Human-readable, hand-editable, git-diffable | ✔ (best with one-object-per-line layout) | ✘ |
| Crash-safe writes | ✔ *if* the temp+rename pattern is adopted (cost ≈ 0) | ✔ built-in |
| Query power (per-ticker selects, aggregates) | none — fine at 500–5,000 rows (load is <10 ms) | ✔ |
| Schema/type enforcement | manual | ✔ with STRICT tables |
| Extra concepts to learn | none | cursors, journal modes, migrations |

**Recommendation:** JSON, one file, transactions one-per-line, written with the
temp+rename pattern, plus a load-time integrity check. Reasons: privacy-by-
readability (the user can see their own data without tools), meaningful diffs in
a public repo, zero new concepts, and the crash risk is eliminated by a pattern
that costs 0.03 ms. The numbers show performance and durability cannot decide
this — readability and simplicity should. SQLite remains fully defensible if
query power or STRICT typing is valued more; nothing measured disqualifies it.

---

## B. Decimal versus float — the error demonstrated

Scenario (all quantised to 4 dp under both arithmetics, so only arithmetic
differs): buy $500 @ 309.90 → buy $750 @ 287.05 → **3:2 split** → buy $300 @
322.40 → sell half @ 341.10. Then 250 further round-trip trades.

### B1. Pure arithmetic divergence — honest result

| After the 5 steps | float | Decimal |
|---|---|---|
| Share count | 3.6349 | 3.6349 |
| Cost basis | $776.495050 | $776.49512871 |
| Realised P/L | $462.369340 | $462.36941871 |

After ~505 transactions total, realised P/L differs by **$0.000027**.

**Plainly: float arithmetic error alone is negligible at portfolio scale.**
Anyone deciding Decimal-vs-float on rounding-drift grounds is arguing about the
wrong mechanism. Here are the three mechanisms that actually bite:

### B2. Mechanism 1 — corrupted prices flip the 4th decimal (the #1 data point)

Issue #1 found `fast_info` reporting Apple at `309.8999938964844` where the raw
endpoint reports `309.9` — a float32 artefact. Ingested unchanged into a
"float costs nothing" model, it changes *stored, permanent* results:

- Buying with cash amounts that hit the 4-dp rounding boundary yields different
  share counts: **3 of 2,900 tested dollar amounts** diverge. Example:
  $2,084 at the true price buys **6.7247 sh**; at the artefact price it buys
  **6.7248 sh** — $0.031 silently mis-recorded *forever* (transactions are facts;
  there is no later correction pass).
- The artefact propagates into every derived average-cost and P/L figure.

This is an *ingestion* problem, not an arithmetic problem: parse prices with
`Decimal(str(value))` at the fetch boundary and it vanishes.

### B3. Mechanism 2 — Python's `round()` disagrees with brokers' rounding

Python uses banker's rounding (half-to-even); financial statements use half-up.
Measured at 4 dp: `round(0.00015, 4)` → `0.0001` vs half-up `0.0002`;
`round(3.33335, 4)` → `3.3333` vs half-up `3.3334`. Two of five boundary values
tested disagree. A position reconciled against a broker statement will
eventually differ by one ulp of the display precision with no bug anywhere.

### B4. Mechanism 3 — the permanent record carries float noise

What `json.dump` writes for ordinary computed values (verified):

```
accumulated cost basis -> 1102349.7103560003
NVDA share count       -> 555.3283000000001
0.1 + 0.2              -> 0.30000000000000004
```

Harmless computationally; corrosive psychologically — every human reading the
ledger must mentally round away noise, in the store whose selling point is
human readability.

### B5. Cost of Decimal

Derivation of 500 transactions: float 0.12 ms, Decimal 0.66 ms — **5.7× slower,
still sub-millisecond**. Interop cost: `json` needs strings, `str(Decimal)`
round-trips exactly. No other practical cost found.

**Recommendation:** money and shares as `Decimal` with explicit quantisation
(`ROUND_HALF_UP`, 4 dp shares / 2 dp money), serialised as strings; prices
converted via `Decimal(str(x))` at ingestion. The case rests on record purity
and rounding-convention correctness, not on drift — drift is a non-issue.

---

## C. FIFO versus weighted average — worked example, then the tax answer

History: buy 10 @ $100, buy 20 @ $150, buy 30 @ $200 (60 sh, $10,000 basis),
then sell 25 @ $220. Identical history, two methods:

| | Weighted average | FIFO |
|---|---|---|
| Cost of sold lots | 25 × $166.6667 = $4,166.67 | 10 × $100 + 15 × $150 = $3,250.00 |
| **Realised profit** | **$1,333.33** | **$2,250.00** |
| Remaining basis | $5,833.33 / 35 sh | $6,750.00 / 35 sh |
| Tax due at PLN 19% rate | differs by **$174.17** between the methods | |

The divergence is large enough that this choice *is* a tax question.

### C1. What Polish tax law requires

**The answer is unambiguous in substance: Poland requires the weighted-average
method for securities.** The chain:

1. **Rozporządzenie Ministra Finansów z dnia 15 grudnia 1995 r. w sprawie
   sposobu prowadzenia podatkowej ewidencji sprzedaży walorów** (Dz.U. 1995 nr
   60, poz. 313; consolidated text Dz.U. 2005 nr 19, poz. 160) — defines the
   tax cost of disposed *walory* (securities) as their acquisition price
   computed by **średnia ważona** (weighted average) over the holding period.
   This is why every Polish broker's PIT-8C reports costs on the weighted-
   average basis.
2. **Ustawa o PIT** supplies the frame: acquisition outlay is the deductible
   cost when determining income from disposal of shares/securities (art. 22),
   foreign-currency transactions convert at the **NBP average rate from the
   working day before the disposal (art. 11a)**, capital gains are taxed at 19%
   on PIT-38. Framework verified against the Dz.U. 2000 nr 14 poz. 176
   consolidated text (mirrored at legalize-dev/legalize-pl, `pl/DU-2000-176.md`,
   line 971: acquisition expenditure is a cost "przy ustalaniu dochodu ze zbycia
   tych... akcji oraz innych papierów wartościowych").
3. **Where the FIFO confusion comes from:** crypto is FIFO under art. 30b
   (costs consumed starting from the earliest purchases). Some brokers' own
   statements use FIFO for their internal lot tracking (verified example:
   Trading212's help centre describes FIFO for its statements — a broker
   convention, *not* the Polish tax rule; contrast verified in OSS tool
   KamilMatejuk/PIT-Trading212 which follows the broker's FIFO presentation).

**Verification status, stated plainly.** This sandbox's network allowlist
blocked `isap.sejm.gov.pl`, `api.sejm.gov.pl`, `podatki.gov.pl` and all Polish
tax-firm sites (403 "no matching allow rule"), so the regulation text itself
could not be re-read tonight; citations above carry exact identifiers so the
check is one lookup. Corroboration actually retrieved: the 2000 consolidated
act text (primary source, older consolidation), an XTB PIT-38 consolidation
tool's README documenting the art. 11a NBP D-1 practice, and PIT-8C behaviour
of Polish brokers (weighted average) vs foreign brokers' statements (FIFO).
**Recommendation: treat weighted average as a constraint, not a preference —
and spend one minute verifying the rozporządzenie citation before v1 locks the
spec.**

### C2. Consequence for the app

Store transactions (the model already does) and **both** methods remain
computable forever, because lots are never destroyed. Compute realised P/L by
weighted average (matches the tax return the user must file); keep the door
open for a FIFO view without schema change. This collapses most of #2's
question 7 into settled ground.

Splits under either method: a 10:1 split turns 100 sh @ avg $50.00 into
1,000 sh @ avg $5.00 — share count divides, per-share price divides, value and
total basis invariant (worked through in `bench/`). Undo of a mistaken split =
enter the inverse ratio as a new transaction; nothing is ever edited in place.

---

## Remaining questions, numbered — each with a recommendation

**Q1. Storage format** — JSON single file vs SQLite. Measured in §A; neither
performance nor durability decides it. **Recommendation:** JSON, one-object-per-
line, atomic temp+rename writes, `.tmp` swept at startup. *(Recommendation only
— this is #2's call.)*

**Q2. Transaction schema** — proposed fields: `id` (monotone int), `ticker`
(str), `kind` (buy/sell/split), `shares` (Decimal-string, required for
buy/sell), `price` (Decimal-string, required for buy/sell), `split_from`,
`split_to` (positive ints, required for split), `date` (ISO-8601, required),
`currency` (required, default "USD" — satisfies the map's currency rule),
`commission` (Decimal-string, optional, default "0.00"), `note` (optional).
Required-vs-optional as listed. **Recommendation:** accept as-is.

**Q3. Split representation** — store numerator/denominator as two integers
(`split_to` : `split_from`), never a float ratio (issue #1 showed raw-endpoint
ratios preserve 3:2 while libraries collapse it to 1.5). Undo by inverse entry.
**Recommendation:** accept; floats banned from ratios.

**Q4. Target weights** — stored *separately* from transactions (they are
policy, not events), same file or sibling file per Q1 outcome. A company sold
down to zero **keeps** its target (deleting it would silently rewrite policy;
the user deletes explicitly). **Recommendation:** separate entity; targets
survive zero positions.

**Q5. Price cache shape** — one snapshot per ticker: `{ticker, price, currency,
quoted_at, fetched_at, source}` per issue #1's two-timestamp finding. Keep only
the latest snapshot in v1; equity curves need history, which belongs to the map's
"Not yet specified". **Recommendation:** latest-only, schema forward-compatible
with appending history.

**Q6. Data file layout** — `~/Library/Application Support/InvestmentPortfolio/portfolio.json`
(outside the repo tree, already settled); env-var override for tests
(suggested name `INVESTMENT_PORTFOLIO_DATA_DIR`); one file, not several
(the whole dataset loads in <1 ms — splitting buys nothing). **Recommendation:**
accept.

**Q7. Position derivation** — fold ordered by `(date, id)`; weighted average
cost; sales realise P/L against current average cost; splits adjust count and
basis-per-share only. Method question settled by §C: weighted average for
display/tax, FIFO computable later. Ordering tie-break matters: same-date
transactions replay in entry order. **Recommendation:** accept.

---

*Scripts producing every number: `bench/` (this branch); see `bench/README.md`
for run order. Run order: `bench_size_time.py`, `bench_gitdiff.py`,
`bench_interrupt.py`, `bench_decimal.py`, `bench_decimal_mech.py`,
`bench_fifo.py`. All numbers above reproduce exactly.*
